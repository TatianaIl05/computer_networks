# Computer_networks
## Task 1
### Пинг 10 доменов, результаты в csv таблице (RTT и 3 других параметра)
## Task 2
### Доступ на любой сервер по SSH без ввода пароля, пуш в удаленный репозитория без ввода пароля. Захватить сетевой трафик с помощью инструмента tcpdump или Wireshark во время подключения по SSHб найти этапы установления соединения (TCP handshake), шифрование данных и завершение сессии
## Task 3
### Парсер, ИСПОЛЬЗУЯ SELENIUM, с пагинацией и авторизацией. результаты в csv таблице (4 параметра). Посмотреть сетевой трафик и проанализировать  различия между HTTP и HTTPS
## Task 4
### Поднять API точку, перейдя по которой, или дернув через curl, передав в параметре url (например: 127.0.0.1/parse?url=https://site.com/page), запустится ваш парсер, отработает, запишет результат в базу PostgresSQL, отдельным методом должны извлекаться данные из ТАБЛИЦЫ БД и отдаваться в виде json.
## Task 5
### Развернуть отдельно 2 докер контейнера, (БЕЗ  docker-compose) и настроить сеть между ними:
* 1-ый контейнер - ваше приложение (на порту отличном от 80)
* 2-ой контейнер - база данных
## Task 6
### Настроить проксирование, таким образом, чтобы запросы на 80 порт перенаправлялись в ваше приложение в докер контейнере
## Task 7
### Прикрутить заглушку по Российскому IP / prefix.  То есть переходя с РФ ip должен сработать РЕДИРЕКТ на любую заглушку вроде "ВАМ СЮДА НЕЛЬЗЯ".
### Важно! Заглушка должна быть реализована на уровне сервера, а не приложения
## Task 8
### Сделать так, чтобы приложение было доступно на стороннем пк по ip.
## Task 9
### Настроить IPv6 на виртуальных машинах или контейнерах и сравнить пакеты IPv4 и IPv6
## Task 10
### Сначала руками в терминале, а затем скриптом на любом ЯП:
Выполнить DNS-запросы для списка доменов.
- Сохранить их IP-адреса.
- Выполнить traceroute для каждого IP-адреса.
- Сохранить результаты в CSV-файл.
